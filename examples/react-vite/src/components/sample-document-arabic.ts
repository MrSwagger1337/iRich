/**
 * Canonical Arabic Editorial sample document for React + Vite example (RTL).
 */

import { arabicEditorialDocument } from '@irich/editorial';
import type { IRichDocument } from '@irich/core';

export const initialArabicViteDocument: IRichDocument = arabicEditorialDocument;
