/**
 * Canonical English Editorial sample document for React + Vite example.
 */

import { redesignedEditorialDocument } from '@irich/editorial';
import type { IRichDocument } from '@irich/core';

export const initialViteDocument: IRichDocument = redesignedEditorialDocument;
