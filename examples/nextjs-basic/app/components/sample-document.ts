/**
 * Canonical English Editorial sample document for Next.js example.
 */

import { redesignedEditorialDocument } from '@irich/editorial';
import type { IRichDocument } from '@irich/core';

export const initialNextjsDocument: IRichDocument = redesignedEditorialDocument;
export const initialDocument: IRichDocument = initialNextjsDocument;
