/**
 * Canonical Arabic Editorial sample document for Next.js example (RTL).
 */

import { arabicEditorialDocument } from '@irich/editorial';
import type { IRichDocument } from '@irich/core';

export const initialArabicNextjsDocument: IRichDocument = arabicEditorialDocument;
export const initialArabicDocument: IRichDocument = initialArabicNextjsDocument;
